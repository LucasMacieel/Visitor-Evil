using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyType : MonoBehaviour
{
    public enum bodyTypeEnum {human, machine}
    public bodyTypeEnum bodyType = bodyTypeEnum.machine;
}
